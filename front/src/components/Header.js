import React, { PureComponent } from "react";
import "./Header.css";

export default class extends PureComponent {
    render() {
        return (
            <header className="component-header">Test Assignment</header>
        );
    }
}
